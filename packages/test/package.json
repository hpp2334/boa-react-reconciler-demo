{
  "name": "@brrd/test",
  "version": "1.0.0",
  "description": "Browser tests for @brrd/core",
  "type": "module",
  "scripts": {
    "test": "pnpm -F core build && vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/browser": "^4.0.3",
    "@vitest/browser-playwright": "^4.0.3",
    "playwright": "^1.48.0",
    "vitest": "^4.0.3"
  },
  "dependencies": {
    "@brrd/core": "workspace:*"
  }
}