import { Config } from "./type"
import code from "./code/todo_list.jsx?raw"

const config: Config = {
    name: "Todo List",
    description: "Interactive todo list with add and remove functionality",
    code
}
export default config