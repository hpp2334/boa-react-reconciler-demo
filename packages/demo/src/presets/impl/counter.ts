import { Config } from "./type"
import code from "./code/counter.jsx?raw"

const config: Config = {
    name: "Counter",
    description: "Simple counter component with increment/decrement buttons",
    code
}
export default config