{
  "name": "boa-react-reconciler-demo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd ./boa-wasm && wasm-pack build && cd .. && pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^18.3.1"
  }
}
